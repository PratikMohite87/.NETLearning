// The class which is created by using the static modifier is called a static class in C#.
// A static class can contain only static members.
// It is not possible to create an instance of a static class.

// In real time project you will have need to store common thing or need to do common task for all object so for that
//      purpose you can use static class.

using Static_Class;

StaticClass.GetNo();
